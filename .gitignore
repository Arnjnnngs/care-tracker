harness/.tmp/
harness/work/

# Render-audit screenshots: regenerated by harness/overflow-scan.mjs on demand, ~55MB a run.
# The gate is outputs/RENDER-<version>.md, which IS committed. The images are evidence for a
# human to look at once, not repository history.
outputs/render-*/
